= simple_form_for @search, :html => {:remote => true}  do |f|
  -# f.inputs :id =>  "ajax_form" do
  =field_set_tag do
    = f.input :company_name_contains, :label => "Name", :required => false
    = f.input :email_contains, :label => "Email", :required => false
    =# f.input :city_contains, :label => "City", :required => false
    =# f.input :country_contains, :label => "Country", :required => false
    =# f.input :zip_contains, :label => "Post Code", :required => false  
  %button.btn.btn-primary{:type=>"submit"} Search
