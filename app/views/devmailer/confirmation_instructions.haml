-content_for :logo do
  %img{:id => "headerImage campaign-icon", "mc:allowtext" => "", "mc:edit" => "header_image", "mc:label" => "header_image", :src => '/assets/images/logo.png', :style => "max-width:180px;"}/
  %h2= VisioInvoice
  
-content_for :header do
  %h1 VisioInvoice Invitation

-content_for :body do
  %table{:width => '600px'}
    %tr
      %td{:align => 'left'}
        %p
        	Welcome
        	= @resource.company.name
        	!
        %p You can confirm your account through the link below:
        %p= link_to 'Confirm my account', confirmation_url(@resource, :confirmation_token => @resource.confirmation_token)

