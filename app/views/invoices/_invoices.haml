%table#table-invoices.table
  %thead
    %tr
      %th
      %th=sort_link @search, :business_id, "Id", {}, :remote => "true"
      %th=sort_link @search, :invoice_date, "Date", {}, :remote => "true"
      %th=sort_link @search, :client_company_name, "Client", {}, :remote => "true"
      %th=sort_link @search, :title, "Title", {}, :remote => "true"      
      %th=sort_link @search, :state, "Status", {}, :remote => "true"
      %th=sort_link @search, :due_days, "Due Days" , {}, :remote => "true"
      %th=sort_link @search, :total_cost_inc_tax_delivery, "Amount", {}, :remote => "true" 
      %th=sort_link @search, :due_amount, "Due", {}, :remote => "true"
      %th
      %th
  %tbody#invoices-body=render :partial => "invoices/invoice", :collection => @invoices
%p= will_paginate @invoices
  

