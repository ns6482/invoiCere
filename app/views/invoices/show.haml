- title "Invoice: " + @invoice.id.to_s + " - " + @invoice.title
#interactive{:class => "modal hide fade"}
#invoice-centre-panel{:class=> "row"}
  #invoice-main{:class=>"span8"}
    = render "invoices/show_elements/show_core", :object =>  @deliveries
  #invoice-side-panel{:class=> "span2"}
    = render "invoices/show_elements/toolbar"